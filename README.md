# markdownConverter
A lightweight Markdown to HTML converter built with JavaScript and regular expressions. This app takes Markdown input from a textarea, parses it with regex rules, and outputs both the raw HTML code and a live rendered preview.
